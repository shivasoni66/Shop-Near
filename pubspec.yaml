name: shop_near
description: "BazarLive social commerce flutter app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  google_fonts: ^6.2.1
  cached_network_image: ^3.3.1
  flutter_svg: ^2.0.10
  intl: ^0.19.0
  shimmer: ^3.0.0
  dio: ^5.4.0
  socket_io_client: ^3.0.1
  flutter_secure_storage: ^9.0.0
  image_picker: ^1.0.7
  camera: ^0.12.0+1
  agora_rtc_engine: ^6.5.3
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
